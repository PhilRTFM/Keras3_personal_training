Aide moi à organiser et optimiser ce prompt selon les specs recommandés par OpenAI met tout dans un bloc de code pour copire coller et n'oublie pas de mentionner les fichiers : 
Tu as les scripts en cours à ta dispositions : 
- 'models.txt' ==> models.R
- 'analysis.txt' ==> analysis.R
- 'metrics.txt' ==> metrics.R
- 'utils.txt' ==> utils.R

- 'run_iris.txt' ==> run_iris.R
- 'run_msleep.txt' ==> run_msleep.R

Soit précis et exhaustif dans toutes les instructions et les structures de données mentionnées 



DEBUT PROMPT : 


Extrait d'une part les bonnes pratiques et les contraintes techniques à adopter dans le cadre de ce développment de projet en R et d'autre part, les notes sur ce que l'on veut développer :

Tu as les scripts en cours à ta dispositions : 
- 'models.txt' ==> models.R
- 'analysis.txt' ==> analysis.R
- 'metrics.txt' ==> metrics.R
- 'utils.txt' ==> utils.R

- 'run_iris.txt' ==> run_iris.R
- 'run_msleep.txt' ==> run_msleep.R


###

Pour le commentaire, il faut uniformiser les conventions,
Ici on privilégie Roxygen


Pas d'importation de paquets et d'installations directes depuis les scripts et notamment les scripts "libs"
==> L'équivalent d'un requirements.txt en python avec la liste de toutes les librairies 
(et un commentaire bref pour rappeler leur usage et leur intéret)

dans toutes les fonctions, appeler la librairie associée par la convention "pkg::fun()" 
 
Explique moi en détail ce code : 

I/O Structures de données Fonctions (I/O) 
Choix des variables et leur signinfications :
==> Le @param de roxygen
1 paramètre par ligne lorsque l'on code une fonction

Bien définir le role de chaque paramètre
TOUJOURS UN RETURN à l'issue de chaque fonction

Pour l'instant ==> supprimer class_weight ==> On ne sait pas son utilité pour l'instant

Verbose ==> en TRUE/FALSE et à toujours la fin (par convention)

Pour la PCA, je veux : 
- Faire les calculs avec FactoMineR et les graphiques avec ggplot2 full custom
- le cercle des corrélation avec les noms des variables pour la pca
- le graph des individus pour la pca

Pour la MDS, je veux : 
==> Déjà, comprendre ce qu'est une putain de MDS xD

- Faire les calculs avec Mass, selon plusieurs modalités de distances (Euclidenne, non euclidienne... etc) à des fins de test et de recherche.
- tout les graphiques nottamement des points de coordonnées de la matrice de distance sur un scratter plot avec ggplot
- Une fonction pour MDS metrique

Bien faire attention à la convention de nomenclature explicite des fonctions dans le dossier R et aux appels spécifiques dans les scripts de test sur des jeux de données dans inst/examples/ 
 
- Une Fonction pour une MDS non-métrique
- La publication de référence pour la MDS : https://bmcbioinformatics.biomedcentral.com/articles/10.1186/s12859-019-2780-y
- Schematic representation of the strategy for multidimensional scaling. a An example positive, symmetric matrix of distance values between four objects. b A dimension-reducing MDS representation of the distances in the matrix. c The stress equation for calculating the overall difference between the distances in the feature space (panel A, di,j) and the distances on the 2D plane (panel B, Di,j)
- Dimension reduction and distnet representation of differences in ex vivo drug sensitivities between hematological cancer patient samples from Majumder et al. [10]. a A standard multidimensional scaling representation of the differences in drug sensitivity between patient samples. b The distances between points in panel A compared to their actual distances in the feature space (a Shepard plot). c A static version of the distnet plot of this dataset, where the lines between points represent point pairs with a distance of 500 or less in the feature space. Circled points show inconsistencies between the feature space distances and distances on the 2D plot. For an interactive version of panel C, visit the supplemental interactive file online [15]
- Calculer et afficher le stress de Kruskal (à 4 Chiffres Significatifs)


Lister toutes les fonctions et décrire leur rôle
Le bouton pour afficher les fonctions dans RSTUDIO.

==> L'arborescence du Git : 
à bien définir en vue du premier commit.

Générer un doc en PDF avec Roxyen



Commit sur GitHub la première version du Pipeline

==> Est-il possible de faire une seul fonction build and fit ? (notion de wrapper : Qu'est-ce qu'un Wrapper ?)


==> Pour les exports de figures : en 16:9, avec devoff et PDF, pas de PNG, et toutes les figures exportés sur différentes pages d'un même PDF en output
Toujours Timestamp les output norme ISO 8601 (YYYY-MM-DD)
mettre en option les éllipses, faire des data points plutôt petits

Factorisation modulaire du code


class_weight ==> à supprimer pour le moment dans models

Structure d'un commentaire type R Oxygen : 
- Nom de la méthode
- breve description
- paramètres et types (1 par ligne)
- ce que cela retourne


Discriminer dans la gestion de projet : 
- Script d'exploration
- Script de consoloidation
==> Retranscrire ça dans les branch du projet GitHub, éventuellement


Pour l'instant, l'architecture du projet est là : 

Keras3_personal_training/
├─ DESCRIPTION                 # Métadonnées du package (Imports, Depends, etc.)
├─ NAMESPACE                   # Généré par roxygen2
├─ R/
│  ├─ models.R                 # Fonctions pour MLP supervisé et AutoEncodeur non-supervisé
│  ├─ analysis.R               # PCA, MDS et visualisations avec ggplot2
│  └─ metrics.R                # Évaluation des modèles et matrices de confusion
├─ man/                        # Documentation Rd générée
├─ inst/
│  └─ examples/                # Scripts d’exemples d'utilisation
│     ├─ run_iris.R            # Test sur le jeu de données iris
│     └─ run_msleep.R          # Test sur le jeu de données msleep
├─ tests/                      # (optionnel) Tests unitaires avec testthat
├─ README.md
└─ .gitignore

Je voudrais l'ammener là : 

Keras3_personal_training/
├─ DESCRIPTION                 # Métadonnées du package (Imports, Depends, etc.)
├─ NAMESPACE                   # Généré par roxygen2
├─ R/
│  ├─ utils.R                  # Fonctions pour preprocessing les jeux données et fonctions de sauvegarde des plots
│  ├─ models.R                 # Fonctions pour MLP supervisé et AutoEncodeur non-supervisé
│  ├─ analysis.R               # PCA, MDS et visualisations avec ggplot2
│  └─ metrics.R                # Évaluation des modèles et matrices de confusion
├─ man/                        # Documentation Rd générée
├─ inst/
│  └─ examples/                # Scripts d’exemples d'utilisation
│     ├─ run_iris.R            # Test sur le jeu de données iris
│     └─ run_msleep.R          # Test sur le jeu de données msleep
├─ README.md
└─ .gitignore


==> Pour Test sur le jeu de données iris : 
- Faire une ACP et une MDS sur iris
- Faire tourner un modèle suppervisé (mlp) et plot la matrice de confusion avec le tableau des métriques
- Faire tourner l'auto-encodeur et pour le tester : 
	🔹 Métriques d’évaluation (numériques + labels)
	-MSE / RMSE → mesure la fidélité globale de reconstruction.
	- MAE → plus robuste aux valeurs extrêmes.
	- AUC-ROC / AUC-PR → si l’objectif est la détection d’anomalies via reconstruction.
	🔹 Graphiques utiles
	- Courbe d’apprentissage (train/val loss) → suivre convergence et surapprentissage.
	- Distribution des erreurs de reconstruction (histogramme/densité) → séparer normales vs anomalies.
	- Scatter plot X vs X' (ou erreur vs label) → visualiser la qualité de reconstruction par classe.
- Pour chacun des deux modèles, on veux aussi les history plot epoch sur Accuracy et epoch sur Loss

- Exporter tout les graphs et résulats dans le PDF de sortie : 
Exemple de méthodologie :

``` 
# Ouvrir un périphérique PDF
pdf("mes_plots.pdf")

# Générer quelques graphiques
plot(1:10, main = "Premier plot")
hist(rnorm(100), main = "Deuxième plot")

# Fermer le périphérique PDF
dev.off()
```



## Structure du jeu de données msleep :
Voici sa structure :

🔹 Description générale
Nombre d’observations : 83 mammifères
Nombre de variables : 11 colonnes
Thème : habitudes de sommeil, caractéristiques biologiques et taxonomiques
🔹 Variables
name (string) – nom commun de l’animal
genus (string) – genre taxonomique
vore (string, catégorielle) – type alimentaire : carnivore, herbivore, omnivore, insectivore (souvent abrégé : carni, herbi, omni, inse)
order (string) – ordre taxonomique
conservation (string) – statut de conservation (ex. lc = least concern, vu = vulnerable, etc.)
sleep_total (numérique) – nombre total d’heures de sommeil par jour
sleep_rem (numérique) – nombre d’heures de sommeil paradoxal (REM) par jour
sleep_cycle (numérique) – durée moyenne d’un cycle de sommeil (en heures)
awake (numérique) – nombre d’heures éveillé par jour (= 24 - sleep_total)
brainwt (numérique) – poids du cerveau (en kg)
bodywt (numérique) – poids du corps (en kg)

==> Pour le Test sur le jeu de données msleep
- Pour le preprocessing on veut d'abord séparer les variables catégorielles des variables numériques : 
- Pour les variables numériques : 
 ==> Cliff's delta  + visualisation
- Pour les variables catégorielles : 
 ==> Correlation matrix, Test du X² ou Fisher


FIN PROMPT

